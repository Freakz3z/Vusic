
// Refactored: No longer handles auto-hide timer.
// Visibility is now controlled by CSS/Hover in components based on `enableImmersiveMode`.
export default function ImmersiveHandler() {
  return null
}
